# Copy this file to .env and fill in real values. Never commit .env itself.

# --- Paths (override defaults in config.py if needed) ---
DATA_DIR=./data
LOG_DIR=./logs

# --- Database / SQL warehouse connection (if used for sql/ layer) ---
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# --- Dashboard ---
STREAMLIT_SERVER_PORT=8501

# --- Misc ---
LOG_LEVEL=INFO
RANDOM_SEED=42
